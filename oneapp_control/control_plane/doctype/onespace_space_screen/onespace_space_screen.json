{
 "actions": [],
 "creation": "2026-08-29 00:00:00.000000",
 "doctype": "DocType",
 "editable_grid": 1,
 "engine": "InnoDB",
 "field_order": [
  "screen",
  "label",
  "icon",
  "cb_view_source",
  "document_type",
  "fields",
  "component",
  "hide_new",
  "status_field",
  "view_types",
  "view_settings",
  "tab_icons",
  "sec_view_query",
  "filters",
  "cb_view_sort",
  "order_by",
  "sec_screen_fixtures",
  "naming_series",
  "cb_screen_print",
  "print_formats"
 ],
 "fields": [
  {
   "description": "Slug in the URL, e.g. `invoices`. Stable: it is what a bookmark points at.",
   "fieldname": "screen",
   "fieldtype": "Data",
   "in_list_view": 1,
   "label": "Screen",
   "reqd": 1
  },
  {
   "description": "Shown in the app's navigation.",
   "fieldname": "label",
   "fieldtype": "Data",
   "in_list_view": 1,
   "label": "Label",
   "reqd": 1
  },
  {
   "default": "lucide-layout-grid",
   "fieldname": "icon",
   "fieldtype": "Select",
   "label": "Icon",
   "options": "lucide-layout-grid\nlucide-database\nlucide-sparkles\nlucide-shield\nlucide-users\nlucide-user-round\nlucide-graduation-cap\nlucide-stethoscope\nlucide-briefcase\nlucide-calendar\nlucide-clock\nlucide-wrench\nlucide-file-text\nlucide-receipt\nlucide-wallet\nlucide-shopping-cart\nlucide-package\nlucide-truck\nlucide-factory\nlucide-store\nlucide-message-square\nlucide-mail\nlucide-phone\nlucide-chart-line\nlucide-chart-pie\nlucide-book-open"
  },
  {
   "fieldname": "cb_view_source",
   "fieldtype": "Column Break"
  },
  {
   "description": "What the list shows. Data rather than a Link: the doctype belongs to a tenant site and need not exist here.",
   "fieldname": "document_type",
   "fieldtype": "Data",
   "in_list_view": 1,
   "label": "Doctype"
  },
  {
   "description": "Comma-separated fieldnames for the columns. Labels and types come from the tenant site's own metadata, so this is the only thing worth writing down. Empty shows the doctype's own list fields.",
   "fieldname": "fields",
   "fieldtype": "Small Text",
   "label": "Fields"
  },
  {
   "description": "Escape hatch: a component the SPA registered under `spaceCode/screen`. Set this and the doctype above is ignored.",
   "fieldname": "component",
   "fieldtype": "Data",
   "label": "Component"
  },
  {
   "default": "0",
   "description": "Hide New on this screen even where the doctype allows one to be made. For a screen that reads records something else writes.",
   "fieldname": "hide_new",
   "fieldtype": "Check",
   "label": "Never offer New"
  },
  {
   "description": "Which field says where a record stands \u2014 the one whose value goes on the badge beside a record's name. A fieldname, checked against the doctype like any other; the colours are the doctype's own Document States, not something to repeat here. Empty is no badge.",
   "fieldname": "status_field",
   "fieldtype": "Data",
   "label": "Status Field"
  },
  {
   "default": "list",
   "description": "How this screen may be looked at, in order \u2014 `list`, `board`, `calendar`, `grid`, `map`. The first is what it opens with. Anything the SPA does not build yet is ignored rather than refused, so a manifest can name one before it ships.",
   "fieldname": "view_types",
   "fieldtype": "Data",
   "label": "View Types"
  },
  {
   "description": "Per type, what that type needs: {\"board\": {\"column_field\": \"status\", \"card_fields\": [\"priority\"]}}. Nested by view type because one screen offers several. A key ending in `_field` is one fieldname and `_fields` is a list of them; both are checked against the doctype like a filter or a sort, and anything else is dropped. A board may name a Select or a Link here instead of the status field \u2014 and a reader may name another again in a saved view, which is where their own choice lives. Each card-shaped type keeps its own `card_fields`: a board card sits under a heading naming the field it is bucketed by and a grid card does not.",
   "fieldname": "view_settings",
   "fieldtype": "Code",
   "label": "View Settings",
   "options": "JSON"
  },
  {
   "description": "An icon per tab of the record form, keyed by the tab's label: {\"Overview\": \"lucide-list\"}. Only where the tab's own words earn the wrong glyph \u2014 every tab has one without this. Names come from the SPA's TAB_ICONS.",
   "fieldname": "tab_icons",
   "fieldtype": "Code",
   "label": "Tab Icons",
   "options": "JSON"
  },
  {
   "fieldname": "sec_view_query",
   "fieldtype": "Section Break",
   "label": ""
  },
  {
   "description": "Always applied, e.g. {\"status\": \"Open\"}.",
   "fieldname": "filters",
   "fieldtype": "Code",
   "label": "Filters",
   "options": "JSON"
  },
  {
   "fieldname": "cb_view_sort",
   "fieldtype": "Column Break"
  },
  {
   "default": "modified desc",
   "fieldname": "order_by",
   "fieldtype": "Data",
   "label": "Order By"
  },
  {
   "fieldname": "sec_screen_fixtures",
   "fieldtype": "Section Break",
   "label": ""
  },
  {
   "description": "Prefixes this screen's doctype offers, one per line \u2014 `ACME-INV-.YYYY.-.#####`. Applied once, when the space first arrives; the workspace edits its own afterwards under Settings, Naming. Ignored where the doctype has no `naming_series` field.",
   "fieldname": "naming_series",
   "fieldtype": "Small Text",
   "label": "Naming Series"
  },
  {
   "fieldname": "cb_screen_print",
   "fieldtype": "Column Break"
  },
  {
   "description": "Print formats to ship, as a list: [{\"name\": \"ACME Invoice\", \"default\": true, \"layout\": {...}, \"setup\": {...}}]. `layout` is Frappe's own `format_data` \u2014 draw it in the builder under Settings, Print formats and paste it here, so what an app ships and what a workspace draws are the same kind of thing. Created once, per format, if nothing of that name exists.",
   "fieldname": "print_formats",
   "fieldtype": "Code",
   "label": "Print Formats",
   "options": "JSON"
  }
 ],
 "index_web_pages_for_search": 1,
 "istable": 1,
 "links": [],
 "modified": "2026-08-29 00:00:00.000000",
 "modified_by": "Administrator",
 "module": "Control Plane",
 "name": "OneSpace Space Screen",
 "owner": "Administrator",
 "permissions": [],
 "sort_field": "modified",
 "sort_order": "DESC",
 "states": [],
 "track_changes": 1
}
