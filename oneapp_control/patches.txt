# Nothing yet, and that is the point: every site there will ever be was
# created from this tree, so there is nothing to migrate forward. A patch
# belongs here the moment a shape changes under a site that already exists.

[pre_model_sync]

[post_model_sync]
